import React from 'react';
import styled from 'styled-components';

const Circle = props => <Container />;

const Container = styled.View`
  width: 50px;
  height: 50px;
  background: white;
  border-radius: 25px;
`;

export default Circle;
